# 새내기 과목픽 — 목업 PRD

> 버전: 1.2  
> 상태: 목업 제작 범위 확정  
> 대상 학과: 수학과 · 데이터사이언스학과

## 1. 제품 개요

### 한 줄 정의

27학번 신입생이 학과를 선택하면, 해당 학과 학생들의 강의 선택 기록을 집계한 통계로 **어떤 강의를 몇 명이 선택했는지와 그 비율**을 보여주는 모바일 목업이다.

### 출발점

설문 원문의 “학과를 선택한다”, “선택 비율을 확인한다”와 관심사인 “통계데이터, AI”를 바탕으로 한다. 첫 목업은 1학년으로 고정하고 수학과와 데이터사이언스학과만 제공한다.

사용자는 학과 하나를 선택한다. 서비스는 해당 학과 학생별 강의 선택 기록을 강의 단위로 집계해, 수집된 강의 전체의 선택 학생 수와 비율을 보여준다. 대표 강의 3개만 고정해서 보여주거나 과목 영역을 별도로 선택하지 않는다.

실제 학생 조사와 학교 데이터 연동은 이번 목업 범위에 포함하지 않는다. 목업에서는 실제 데이터와 같은 구조의 가상 학생별 선택 기록과 집계 결과를 사용한다.

## 2. 목표와 범위

### 목표

- 관심 학과 하나를 선택하는 짧은 과정을 체험한다.
- 해당 학과에서 수집된 강의별 선택 학생 수와 비율을 확인한다.
- 상위 3개로 제한하지 않고, 데이터에 포함된 강의를 선택 비율순으로 모두 확인한다.
- 한 학생이 여러 강의를 선택할 수 있다는 통계 기준을 명확히 전달한다.
- 다른 학과를 선택하면 통계 전체가 해당 학과 데이터로 바뀌는 것을 확인한다.
- 390px 모바일 화면에서 긴 강의 목록과 막대그래프를 가로 넘침 없이 읽을 수 있다.
- 모든 정보가 실제 통계나 수강 추천이 아닌 가상 시연 데이터임을 명확히 알린다.

### 핵심 사용자

- 27학번 신입생
- 같은 학과 학생들이 어떤 강의를 많이 선택했는지 통계로 살펴보고 싶은 학생

## 3. 킬러 기능

**학과를 선택하면 학생별 강의 선택 기록을 집계해, 수집된 모든 강의의 선택 학생 수와 비율을 순위로 보여준다.**

### 핵심 체험 흐름

1. 학과 목록에서 수학과 또는 데이터사이언스학과를 선택한다.
2. 선택 즉시 해당 학과의 통계 화면으로 이동한다.
3. 조사 인원과 복수 선택 가능 여부를 확인한다.
4. 강의별 선택 학생 수, 비율과 막대그래프를 비교한다.
5. 아래로 스크롤해 데이터에 포함된 강의 전체를 확인한다.
6. `다른 학과 보기`를 눌러 돌아간 뒤 다른 학과의 통계를 확인한다.

## 4. 통계 기준

### 원본 데이터 단위

학생 한 명의 기록에는 학과와 그 학생이 선택한 강의 목록이 들어간다.

```ts
type StudentCourseRecord = {
  studentId: string;
  department: Department;
  selectedCourseIds: string[];
};
```

### 집계 방법

- **응답 학생 수:** 선택한 학과에서 유효한 응답을 제출한 고유 학생 수
- **강의 선택 학생 수:** 해당 강의를 선택한 고유 학생 수
- **강의 선택 비율:** `강의 선택 학생 수 ÷ 응답 학생 수 × 100`
- 학생 한 명이 여러 강의를 선택할 수 있다.
- 같은 학생이 같은 강의를 중복 선택한 기록은 한 번만 센다.
- 강의별 비율 합계는 100%가 아닐 수 있으며, 일반적으로 100%를 넘을 수 있다.
- 비율은 소수점 첫째 자리까지 표시한다.
- 선택 학생 수가 많은 순서로 정렬하며, 동률이면 강의명 가나다순으로 정렬한다.
- 데이터에 포함된 강의를 상위 3개로 자르지 않고 모두 표시한다.

## 5. 화면 구성

### 화면 1 — 학과 선택

#### 상단

- 제목: `우리 학과 학생들은 어떤 강의를 선택했을까요?`
- 배지: `1학년 · 가상 수강 선택 통계`
- 설명: `학과를 선택하면 학생별 강의 선택 기록을 집계한 결과를 보여드려요.`
- 안내: 통계와 강의명은 실제 조사 또는 실제 개설 정보가 아니라는 점을 첫 화면부터 표시한다.

#### 학과 목록

- 수학과
- 데이터사이언스학과
- 각 학과를 독립된 카드로 표시한다.
- 학과 카드를 누르면 별도의 영역 선택 없이 바로 통계 화면으로 이동한다.
- 터치 직후 선택 카드에 민트색 강조와 체크 표시를 보여준 뒤 화면을 전환할 수 있다.

### 화면 2 — 학과별 강의 선택 통계

#### 상단 요약

- 선택한 학과명
- `1학년`
- 조사 기준일 또는 `목업 데이터` 표시
- 응답 학생 수
- 복수 강의 선택 가능
- 집계 기준 설명: `비율은 전체 응답 학생 중 해당 강의를 선택한 학생의 비율입니다.`
- 제목 예시: `수학과 학생들이 선택한 강의`

#### 통계 목록

선택한 학과의 데이터에 포함된 강의를 선택 학생 수 내림차순으로 모두 표시한다.

- 순위
- 강의명
- 선택 학생 수
- 전체 응답 학생 대비 비율
- 0–100% 동일 척도의 둥근 가로 막대

비율 숫자는 막대 끝에 가깝게 배치하되, 좁은 화면에서도 잘리거나 겹치지 않아야 한다. 목록이 길어지면 페이지 전체를 세로로 스크롤한다.

#### 통계 해석 안내

`한 학생이 여러 강의를 선택할 수 있어 비율의 합은 100%를 넘을 수 있습니다.`

#### 하단 행동

- `다른 학과 보기` 버튼
- 누르면 학과 선택 화면으로 돌아간다.
- 이전에 선택한 학과 카드는 선택 상태를 유지해도 된다.
- 다른 학과를 선택하면 학과명, 응답 학생 수와 강의별 통계 전체를 갱신한다.

#### 고정 안내

`실제 개설 강의·수강 신청률·수강 추천이 아닙니다.`

## 6. 가상 샘플 집계 데이터

### 공통 규칙

- 각 학과의 가상 응답 학생은 120명이다.
- 학생 한 명은 여러 강의를 선택할 수 있다.
- 아래 표는 가상 학생별 기록을 집계한 결과다.
- 비율은 `선택 학생 수 ÷ 120명 × 100`으로 계산하고 소수점 첫째 자리까지 반올림한다.
- 선택 학생 수와 비율의 합은 120명 또는 100%가 될 필요가 없다.
- 실제 조사나 개인 기록은 없다.
- 학과명과 강의명도 목업을 위해 작성한 가상 예시다.

### 수학과 — 응답 학생 120명

| 순위 | 강의명 | 선택 학생 수 | 선택 비율 |
|---:|---|---:|---:|
| 1 | 미적분학입문 | 98명 | 81.7% |
| 2 | 선형대수기초 | 86명 | 71.7% |
| 3 | 수학적사고와증명 | 74명 | 61.7% |
| 4 | 이산수학입문 | 59명 | 49.2% |
| 5 | 기초통계학 | 41명 | 34.2% |
| 6 | 수학프로그래밍 | 25명 | 20.8% |

### 데이터사이언스학과 — 응답 학생 120명

| 순위 | 강의명 | 선택 학생 수 | 선택 비율 |
|---:|---|---:|---:|
| 1 | 데이터사이언스입문 | 101명 | 84.2% |
| 2 | 파이썬프로그래밍기초 | 92명 | 76.7% |
| 3 | 통계학기초 | 79명 | 65.8% |
| 4 | 데이터시각화입문 | 66명 | 55.0% |
| 5 | 데이터베이스기초 | 48명 | 40.0% |
| 6 | 인공지능개론 | 31명 | 25.8% |

## 7. 상태와 상호작용 규칙

| 상태 또는 행동 | 결과 |
|---|---|
| 수학과 카드 선택 | 수학과 응답 학생의 강의 선택 통계 전체를 표시한다. |
| 데이터사이언스학과 카드 선택 | 데이터사이언스학과 응답 학생의 강의 선택 통계 전체를 표시한다. |
| 통계 화면 진입 | 선택한 학과와 정확히 일치하는 집계 결과만 표시한다. |
| 강의 목록 표시 | 선택 학생 수 내림차순으로 모든 강의를 표시한다. |
| 긴 목록 | 모바일 화면에서 세로 스크롤로 전체 강의를 확인한다. |
| 다른 학과 보기 | 학과 선택 화면으로 돌아간다. |
| 다른 학과 선택 | 학과명·응답 수·순위·강의명·선택 학생 수·비율·막대를 모두 갱신한다. |
| 선택값 없이 결과 URL 접근 | 학과 선택 화면으로 이동하고 `먼저 학과를 선택해 주세요.`라고 안내한다. |
| 데이터가 없는 학과 | `아직 집계된 강의 선택 데이터가 없습니다.`라는 빈 상태를 표시한다. |

## 8. 시각 디자인 방향

- 기준 화면: 모바일 390px, 단일 열
- 정보 위계: 제목 → 학과 선택 → 조사 기준 → 강의별 통계
- 배경: 옅은 회백색
- 카드: 흰색
- 주요 강조색: 신뢰감 있는 남색
- 선택 상태: 민트색
- 서체: Pretendard 계열, 제목은 굵게, 본문은 읽기 쉬운 크기와 줄 간격 적용
- 대표 요소: 선택 비율 숫자가 끝부분에 붙는 둥근 가로 막대그래프
- 사진은 사용하지 않는다.
- 터치 대상은 모바일에서 누르기 쉽도록 최소 높이 44px을 권장한다.

## 9. 가상 데이터 고지

두 화면 모두 다음 내용을 쉽게 발견할 수 있는 위치에 표시한다.

> 본 서비스의 학과·강의명·응답 학생 수·선택 비율은 목업 체험을 위한 완전한 가상 예시이며, 실제 재학생 조사·학교 개설 강의·수강 신청률·수강 추천을 의미하지 않습니다.

## 10. 기술 구현 범위

- Next.js
- TypeScript
- 가상 학생별 선택 기록 또는 동일한 결과를 내는 정적 집계 데이터 사용
- 클라이언트 화면 상태로 학과 선택과 결과 화면 연결
- 모바일 390px 우선 반응형 구현
- Supabase, 로그인, 데이터베이스, 외부 API는 사용하지 않는다.
- Vercel 배포는 공유가 필요할 때 별도로 선택한다.

### 권장 타입

```ts
type Department = 'mathematics' | 'data-science';

type StudentCourseRecord = {
  studentId: string;
  department: Department;
  selectedCourseIds: string[];
};

type CourseStatistic = {
  rank: number;
  courseId: string;
  courseName: string;
  selectedStudentCount: number;
  percentage: number;
};

type DepartmentStatistics = {
  department: Department;
  respondentCount: number;
  courses: CourseStatistic[];
};
```

## 11. 이번에 만들지 않을 것

- 전공·교양 등 과목 영역 선택
- 결과를 상위 3개 강의로 제한하는 기능
- 여러 조건을 조합한 결과 필터링
- 실제 학생 대상 설문 수집 화면과 실시간 통계 갱신
- 로그인·회원가입·학교 인증
- 개인 시간표 저장
- 학생 개인 기록 노출
- AI 추천
- 실제 학교의 개설 강의 또는 수강 신청 정보 연동
- 관리자 기능

## 12. 완료 기준

- [ ] 학과 선택 화면과 통계 화면, 총 2개 화면을 클릭 체험할 수 있다.
- [ ] 수학과 카드를 누르면 수학과 학생들의 강의 선택 통계가 표시된다.
- [ ] 데이터사이언스학과 카드를 누르면 해당 학과 학생들의 강의 선택 통계가 표시된다.
- [ ] 결과가 3개로 제한되지 않고 샘플 데이터의 강의 전체를 보여준다.
- [ ] 각 강의의 선택 학생 수와 비율이 샘플 데이터 및 계산식과 일치한다.
- [ ] `응답 학생 수`, `복수 선택 가능`, `비율 합계가 100%가 아닐 수 있음`이 명확히 표시된다.
- [ ] 학생별 기록은 화면에 노출하지 않고 집계 결과만 보여준다.
- [ ] 다른 학과를 선택하면 결과의 모든 통계 항목이 해당 학과 데이터로 갱신된다.
- [ ] 390px에서 가로 넘침 없이 그래프와 수치가 읽히고 전체 목록을 세로 스크롤할 수 있다.
- [ ] 두 화면에서 가상 통계이며 실제 개설 강의·수강 신청률·추천이 아님을 알 수 있다.

## 13. 60–90초 시연 시나리오

총 75초를 기준으로 한다.

1. 문제와 가상 데이터 고지 소개 — 10초
2. 학과 목록에서 수학과 선택 — 10초
3. 응답 학생 수와 복수 선택 집계 기준 설명 — 15초
4. 강의별 선택 학생 수·비율과 전체 목록 확인 — 20초
5. 다른 학과로 바꿔 통계가 갱신되는 모습 확인 — 10초
6. 개인 기록 비노출과 실제 데이터가 아니라는 한계 설명 — 10초

